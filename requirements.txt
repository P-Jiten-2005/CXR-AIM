fastapi>=0.110.0
uvicorn>=0.28.0
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0
asyncpg>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-multipart>=0.0.9
opencv-python>=4.9.0.80
numpy>=1.26.4
websockets>=12.0
ultralytics>=8.1.0
sahi>=0.11.14

pupil-apriltags>=1.0.4

# --- GPU-enabled PyTorch (CUDA 12.4) ---
# ultralytics pulls a CPU-only torch by default. To train/infer on an NVIDIA GPU,
# install the CUDA build from the PyTorch index. If you have no NVIDIA GPU, skip the
# --extra-index-url line below and a normal CPU torch will be used automatically.
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.6.0
torchvision>=0.21.0
